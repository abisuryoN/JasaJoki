{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": ""
    },
    "deploy": {
        "startCommand": "cd backend && php artisan migrate --force && php artisan db:seed --force && php artisan tinker --execute=\"App\\\\Models\\\\User::where('email','admin@dualcode.id')->update(['password'=>bcrypt('admin123')])\" && php artisan serve --host=0.0.0.0 --port=$PORT",
        "restartPolicyType": "ON_FAILURE"
    }
}